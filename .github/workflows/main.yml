name: Multiple Jobs
on:
  push:
    branches:
      - master
  # schedule:
  #   - cron: "*/5 * * * *" # every 5 minutes
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run script 1
        run: node script_1.js

      - name: Run script 2
        run: node script_2.js
